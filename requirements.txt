fastapi==0.115.0
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
# uvicorn is intentionally not pinned here; it is only needed to run the app locally
# and was already available in the working environment.